# Handoff

## 目标

本轮用户目标：
- 阅读并整理 `include` 下头文件
- 合并相似功能头文件的入口
- 修复外部调用变更
- 按代码风格规范 PDF 的“3.1 文件注释”补头文件注释
- 在不影响性能和原功能前提下，清理项目中的冗余代码与可等价简化的复杂代码块

后续用户明确要求：
- 全程中文
- 持续按这些 skill 的风格工作：`caveman`、`setup-matt-pocock-skills`、`zoom-out`、`diagnose`、`grill-me`、`improve-codebase-architecture`

最后用户要求：
- 使用 `handoff` 整理当前状态

## 已完成

### 1. 统一入口头文件

新增：
- `include/CameraTask/CameraTask.hpp`
- `include/ImageRecognize/TargetTracking.hpp`
- `include/KalmanFilter/KalmanFilters.hpp`
- `include/NetworkTask/NetworkTask.hpp`
- `include/SerialTask/SerialTask.hpp`

说明：
- 没有硬合并实现，只做“统一入口头文件”收口
- 这样风险低，外部调用改动小

### 2. 外部 include 已切换

已调整调用方：
- `src/ImagePredict.cc`
- `src/SendTest.cc`
- `src/ReadTest.cc`
- `src/device_a_server.cc`
- `src/device_b_client.cc`
- `include/Tools/AngleCalculate.hpp`

### 3. 文件头注释

已按 PDF “3.1 文件注释”要求补头文件开头 `@file` + `@brief`。

说明：
- PDF 已用 `pypdf` 提取过目标条目
- 提取结果确认要点：文件开头写完整文件路径、文件名、简介

### 4. setup-matt-pocock-skills 交互状态

已收集到用户选择：
- Issue tracker: `GitHub`
- Triage labels: `默认`
- Domain docs 布局：还没完成，用户尚未回复最后一步时转向了代码清理任务

注意：
- `setup-matt-pocock-skills` 还没真正写文件
- 仓库当前无：
  - `AGENTS.md`
  - `CLAUDE.md`
  - `CONTEXT.md`
  - `CONTEXT-MAP.md`
  - `docs/adr/`
  - `.scratch/`

### 5. 已完成的保守冗余清理

只动了“确定未使用”或“等价重复”代码。

已清理：
- `include/Tools/AngleCalculate.hpp`
  - 删除未使用 `InterpolateAngleDeg`
- `include/Tools/ScanController.hpp`
  - 删除未使用 `ScanAxis`
  - 删除未使用 `ToString(ScanAxis)`
  - 将原点 yaw/pitch 重复逻辑收成私有 helper：
    - `OriginYawDeg_()`
    - `OriginPitchDeg_()`
- `include/SerialTask/SerialRead.hpp`
  - 删除未使用 `ToEulerAngles`
  - 删除无用依赖 `#include "SerialTask/SerialConfig.hpp"`
- `include/KalmanFilter/CubatureKalmanFilter.hpp`
  - 删除未使用别名 `using CKF = CubatureKalmanFilter`
- `include/Tools/CpuAffinity.hpp`
  - 删除无用头 `#include <cctype>`
- `include/SerialTask/SerialSend.hpp`
  - 删除无用头 `#include <iomanip>`
  - 删除无用头 `#include <iostream>`
- `include/ImageRecognize/ImageShow.hpp`
  - 删除无用头 `#include "opencv2/highgui.hpp"`

## 发现但未处理

### 1. `src/ImagePredict.cc` 仍然过大

现状：
- 这是项目主流程中心
- 线程控制、图像流程、IMU、串口发送、扫描模式、性能统计都堆在一个文件
- 文件很深，不利于测试和 AI 导航

这是下一轮最值得做的架构点。

建议拆分方向：
- 提取串口发送状态机模块
- 提取扫描模式控制模块
- 提取图像主循环中的“阶段切换/目标选择/预测/控制输出”子流程

### 2. `setup-matt-pocock-skills` 没收尾

如果下个 session 要继续工程 skill 体系，需补：
- 选择创建 `AGENTS.md` 还是 `CLAUDE.md`
- 写 `docs/agents/issue-tracker.md`
- 写 `docs/agents/triage-labels.md`
- 写 `docs/agents/domain.md`

### 3. 构建验证受环境阻塞

尝试情况：
- `cmake --build build` 失败
  - 原因：现有 `build/CMakeCache.txt` 指向旧路径 `d:/code/cpp/robomaster`
- 新建目录 `build_codex_check` 配置失败
  - 原因：本机缺少 `Eigen3Config.cmake`

结论：
- 不能在当前环境给出完整编译通过证明
- 当前改动主要是头文件整理、引用收口、无调用代码删除
- 未发现因为这些变更导致的明确语义冲突

## 工作区状态提醒

工作区不是干净的，存在用户/既有改动：
- `.gitignore`
- `camera_runtime_params.ini`
- `src/model/antidrone_stage3_int8_openvino_model/antidrone_stage3.xml`
- `third_lib/kalman` 处于修改状态

处理原则：
- 不要回滚这些改动
- 下一位 agent 继续工作时，避免误把这些当成当前任务引入的变更

## 建议下一步

优先级建议：

1. 做第 2 轮保守清理
- 继续只删“项目内确定未使用”的私有 helper、include、重复逻辑
- 不碰行为路径

2. 做 `ImagePredict.cc` 受控拆分
- 这是最大深挖点
- 适合 `zoom-out` + `improve-codebase-architecture` + `grill-me`

3. 收尾 `setup-matt-pocock-skills`
- 如果后续要长期用 `diagnose` / `triage` / `to-issues`

## 推荐下个 session 使用的 skill

- `caveman`
  - 用户明确要求持续开启
- `zoom-out`
  - 先给 `ImagePredict.cc` 做模块地图
- `improve-codebase-architecture`
  - 用来决定 `ImagePredict.cc` 拆分 seam
- `grill-me`
  - 拆分前一问一答确认设计
- `diagnose`
  - 如果拆分后要保守验证行为一致性
- `setup-matt-pocock-skills`
  - 仅当要继续补工程上下文文件时

## 关键信息摘要

- 用户希望：清理冗余，不影响性能，不改变原功能
- 当前最安全收益已拿到一批
- 当前最大结构问题：`src/ImagePredict.cc` 太大
- 当前最大环境问题：构建缓存路径错误 + 缺 `Eigen3` CMake 配置
